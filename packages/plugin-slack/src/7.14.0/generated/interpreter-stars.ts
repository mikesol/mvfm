// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { Interpreter, TypedNode } from "@mvfm/core";
import { defineInterpreter, eval_ } from "@mvfm/core";
import type { SLACK_NODE_KINDS_STARS } from "./node-kinds-stars";

type SlackStarsKind = (typeof SLACK_NODE_KINDS_STARS)[number];

interface SlackStarsBaseNode<K extends SlackStarsKind = SlackStarsKind> extends TypedNode<unknown> {
  kind: K;
  params?: TypedNode<Record<string, unknown>> | null;
  config: { token: string };
}

export interface SlackStarsAddNode extends SlackStarsBaseNode<"slack/stars_add"> {}
export interface SlackStarsListNode extends SlackStarsBaseNode<"slack/stars_list"> {}
export interface SlackStarsRemoveNode extends SlackStarsBaseNode<"slack/stars_remove"> {}

export const NODE_TO_METHOD_STARS: Record<string, string> = {
  "slack/stars_add": "stars.add",
  "slack/stars_list": "stars.list",
  "slack/stars_remove": "stars.remove",
};

declare module "@mvfm/core" {
  interface NodeTypeMap {
    "slack/stars_add": SlackStarsAddNode;
    "slack/stars_list": SlackStarsListNode;
    "slack/stars_remove": SlackStarsRemoveNode;
  }
}

interface SlackClientLike {
  apiCall(method: string, params?: Record<string, unknown>): Promise<unknown>;
}

export function createSlackStarsInterpreter(client: SlackClientLike): Interpreter {
  const handler = async function* (node: SlackStarsBaseNode) {
    const method = NODE_TO_METHOD_STARS[node.kind];
    if (!method) throw new Error(`Slack interpreter: unknown node kind "${node.kind}"`);
    const params = node.params != null ? yield* eval_(node.params) : undefined;
    return await client.apiCall(method, params);
  };

  return defineInterpreter<SlackStarsKind>()({
    "slack/stars_add": async function* (node: SlackStarsAddNode) {
      return yield* handler(node);
    },
    "slack/stars_list": async function* (node: SlackStarsListNode) {
      return yield* handler(node);
    },
    "slack/stars_remove": async function* (node: SlackStarsRemoveNode) {
      return yield* handler(node);
    },
  });
}
