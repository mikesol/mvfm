// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { CExpr } from "@mvfm/core";
import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsUsergroups } from "./types-usergroups";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

const mk = makeCExpr as <O, Kind extends string, Args extends readonly unknown[]>(
  kind: Kind,
  args: readonly unknown[],
) => CExpr<O, Kind, Args>;

export function buildSlackUsergroups(): SlackMethodsUsergroups {
  return {
    users: {
      list(params) {
        if (params != null) return mk("slack/usergroups_users_list", [liftArg(params)]);
        return mk("slack/usergroups_users_list", []);
      },
      update(params) {
        if (params != null) return mk("slack/usergroups_users_update", [liftArg(params)]);
        return mk("slack/usergroups_users_update", []);
      },
    },
    create(params) {
      if (params != null) return mk("slack/usergroups_create", [liftArg(params)]);
      return mk("slack/usergroups_create", []);
    },
    disable(params) {
      if (params != null) return mk("slack/usergroups_disable", [liftArg(params)]);
      return mk("slack/usergroups_disable", []);
    },
    enable(params) {
      if (params != null) return mk("slack/usergroups_enable", [liftArg(params)]);
      return mk("slack/usergroups_enable", []);
    },
    list(params?) {
      if (params != null) return mk("slack/usergroups_list", [liftArg(params)]);
      return mk("slack/usergroups_list", []);
    },
    update(params) {
      if (params != null) return mk("slack/usergroups_update", [liftArg(params)]);
      return mk("slack/usergroups_update", []);
    },
  };
}
