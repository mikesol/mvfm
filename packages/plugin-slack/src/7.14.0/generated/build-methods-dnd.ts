// AUTO-GENERATED by scripts/codegen.ts — do not edit

import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsDnd } from "./types-dnd";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

export function buildSlackDnd(): SlackMethodsDnd {
  return {
    endDnd(params?) {
      if (params != null) return makeCExpr("slack/dnd_endDnd", [liftArg(params)]);
      return makeCExpr("slack/dnd_endDnd", []);
    },
    endSnooze(params?) {
      if (params != null) return makeCExpr("slack/dnd_endSnooze", [liftArg(params)]);
      return makeCExpr("slack/dnd_endSnooze", []);
    },
    info(params?) {
      if (params != null) return makeCExpr("slack/dnd_info", [liftArg(params)]);
      return makeCExpr("slack/dnd_info", []);
    },
    setSnooze(params) {
      if (params != null) return makeCExpr("slack/dnd_setSnooze", [liftArg(params)]);
      return makeCExpr("slack/dnd_setSnooze", []);
    },
    teamInfo(params) {
      if (params != null) return makeCExpr("slack/dnd_teamInfo", [liftArg(params)]);
      return makeCExpr("slack/dnd_teamInfo", []);
    },
  };
}
