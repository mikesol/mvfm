// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { CExpr } from "@mvfm/core";
import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsCalls } from "./types-calls";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

const mk = makeCExpr as <O, Kind extends string, Args extends readonly unknown[]>(
  kind: Kind,
  args: readonly unknown[],
) => CExpr<O, Kind, Args>;

export function buildSlackCalls(): SlackMethodsCalls {
  return {
    participants: {
      add(params) {
        if (params != null) return mk("slack/calls_participants_add", [liftArg(params)]);
        return mk("slack/calls_participants_add", []);
      },
      remove(params) {
        if (params != null) return mk("slack/calls_participants_remove", [liftArg(params)]);
        return mk("slack/calls_participants_remove", []);
      },
    },
    add(params) {
      if (params != null) return mk("slack/calls_add", [liftArg(params)]);
      return mk("slack/calls_add", []);
    },
    end(params) {
      if (params != null) return mk("slack/calls_end", [liftArg(params)]);
      return mk("slack/calls_end", []);
    },
    info(params) {
      if (params != null) return mk("slack/calls_info", [liftArg(params)]);
      return mk("slack/calls_info", []);
    },
    update(params) {
      if (params != null) return mk("slack/calls_update", [liftArg(params)]);
      return mk("slack/calls_update", []);
    },
  };
}
