// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { PluginContext } from "@mvfm/core";
import type { SlackConfig } from "./types";
import type { SlackMethodsCalls } from "./types-calls";

export function buildSlackCalls(ctx: PluginContext, config: SlackConfig): SlackMethodsCalls {
  const resolveParams = (params: unknown) => (params != null ? ctx.lift(params).__node : null);

  return {
    participants: {
      add(params) {
        return ctx.expr({
          kind: "slack/calls_participants_add",
          params: resolveParams(params),
          config,
        });
      },
      remove(params) {
        return ctx.expr({
          kind: "slack/calls_participants_remove",
          params: resolveParams(params),
          config,
        });
      },
    },
    add(params) {
      return ctx.expr({ kind: "slack/calls_add", params: resolveParams(params), config });
    },
    end(params) {
      return ctx.expr({ kind: "slack/calls_end", params: resolveParams(params), config });
    },
    info(params) {
      return ctx.expr({ kind: "slack/calls_info", params: resolveParams(params), config });
    },
    update(params) {
      return ctx.expr({ kind: "slack/calls_update", params: resolveParams(params), config });
    },
  };
}
