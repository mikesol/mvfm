// AUTO-GENERATED by scripts/codegen.ts — do not edit

import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsAdminApps } from "./types-admin-apps";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

export function buildSlackAdminApps(): SlackMethodsAdminApps {
  return {
    activities: {
      list(params?) {
        if (params != null) return makeCExpr("slack/admin_apps_activities_list", [liftArg(params)]);
        return makeCExpr("slack/admin_apps_activities_list", []);
      },
    },
    approved: {
      list(params) {
        return makeCExpr("slack/admin_apps_approved_list", [liftArg(params)]);
      },
    },
    config: {
      lookup(params) {
        return makeCExpr("slack/admin_apps_config_lookup", [liftArg(params)]);
      },
      set(params) {
        return makeCExpr("slack/admin_apps_config_set", [liftArg(params)]);
      },
    },
    requests: {
      cancel(params) {
        return makeCExpr("slack/admin_apps_requests_cancel", [liftArg(params)]);
      },
      list(params) {
        return makeCExpr("slack/admin_apps_requests_list", [liftArg(params)]);
      },
    },
    restricted: {
      list(params) {
        return makeCExpr("slack/admin_apps_restricted_list", [liftArg(params)]);
      },
    },
    approve(params) {
      return makeCExpr("slack/admin_apps_approve", [liftArg(params)]);
    },
    clearResolution(params) {
      return makeCExpr("slack/admin_apps_clearResolution", [liftArg(params)]);
    },
    restrict(params) {
      return makeCExpr("slack/admin_apps_restrict", [liftArg(params)]);
    },
    uninstall(params) {
      return makeCExpr("slack/admin_apps_uninstall", [liftArg(params)]);
    },
  };
}
