// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { CExpr } from "@mvfm/core";
import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsOauth } from "./types-oauth";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

const mk = makeCExpr as <O, Kind extends string, Args extends readonly unknown[]>(
  kind: Kind,
  args: readonly unknown[],
) => CExpr<O, Kind, Args>;

export function buildSlackOauth(): SlackMethodsOauth {
  return {
    v2: {
      access(params) {
        if (params != null) return mk("slack/oauth_v2_access", [liftArg(params)]);
        return mk("slack/oauth_v2_access", []);
      },
      exchange(params) {
        if (params != null) return mk("slack/oauth_v2_exchange", [liftArg(params)]);
        return mk("slack/oauth_v2_exchange", []);
      },
    },
    access(params) {
      if (params != null) return mk("slack/oauth_access", [liftArg(params)]);
      return mk("slack/oauth_access", []);
    },
  };
}
