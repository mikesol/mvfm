// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { Interpreter, RuntimeEntry } from "@mvfm/core";

export const NODE_TO_METHOD_ADMIN_EMOJI: Record<string, string> = {
  "slack/admin_emoji_add": "admin.emoji.add",
  "slack/admin_emoji_addAlias": "admin.emoji.addAlias",
  "slack/admin_emoji_list": "admin.emoji.list",
  "slack/admin_emoji_remove": "admin.emoji.remove",
  "slack/admin_emoji_rename": "admin.emoji.rename",
};

interface SlackClientLike {
  apiCall(method: string, params?: Record<string, unknown>): Promise<unknown>;
}

export function createSlackAdminEmojiInterpreter(client: SlackClientLike): Interpreter {
  const handlers: Interpreter = {};

  for (const [kind, method] of Object.entries(NODE_TO_METHOD_ADMIN_EMOJI)) {
    handlers[kind] = async function* (entry: RuntimeEntry) {
      const params = entry.children.length > 0 ? yield 0 : undefined;
      return await client.apiCall(method, params as Record<string, unknown>);
    };
  }

  return handlers;
}
