// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { PluginContext } from "@mvfm/core";
import type { SlackConfig } from "./types";
import type { SlackMethodsConversations } from "./types-conversations";

export function buildSlackConversations(
  ctx: PluginContext,
  config: SlackConfig,
): SlackMethodsConversations {
  const resolveParams = (params: unknown) => (params != null ? ctx.lift(params).__node : null);

  return {
    canvases: {
      create(params) {
        return ctx.expr({
          kind: "slack/conversations_canvases_create",
          params: resolveParams(params),
          config,
        });
      },
    },
    externalInvitePermissions: {
      set(params) {
        return ctx.expr({
          kind: "slack/conversations_externalInvitePermissions_set",
          params: resolveParams(params),
          config,
        });
      },
    },
    requestSharedInvite: {
      approve(params) {
        return ctx.expr({
          kind: "slack/conversations_requestSharedInvite_approve",
          params: resolveParams(params),
          config,
        });
      },
      deny(params) {
        return ctx.expr({
          kind: "slack/conversations_requestSharedInvite_deny",
          params: resolveParams(params),
          config,
        });
      },
      list(params?) {
        return ctx.expr({
          kind: "slack/conversations_requestSharedInvite_list",
          params: params != null ? resolveParams(params) : null,
          config,
        });
      },
    },
    acceptSharedInvite(params) {
      return ctx.expr({
        kind: "slack/conversations_acceptSharedInvite",
        params: resolveParams(params),
        config,
      });
    },
    approveSharedInvite(params) {
      return ctx.expr({
        kind: "slack/conversations_approveSharedInvite",
        params: resolveParams(params),
        config,
      });
    },
    archive(params) {
      return ctx.expr({
        kind: "slack/conversations_archive",
        params: resolveParams(params),
        config,
      });
    },
    close(params) {
      return ctx.expr({ kind: "slack/conversations_close", params: resolveParams(params), config });
    },
    create(params) {
      return ctx.expr({
        kind: "slack/conversations_create",
        params: resolveParams(params),
        config,
      });
    },
    declineSharedInvite(params) {
      return ctx.expr({
        kind: "slack/conversations_declineSharedInvite",
        params: resolveParams(params),
        config,
      });
    },
    history(params) {
      return ctx.expr({
        kind: "slack/conversations_history",
        params: resolveParams(params),
        config,
      });
    },
    info(params) {
      return ctx.expr({ kind: "slack/conversations_info", params: resolveParams(params), config });
    },
    invite(params) {
      return ctx.expr({
        kind: "slack/conversations_invite",
        params: resolveParams(params),
        config,
      });
    },
    inviteShared(params) {
      return ctx.expr({
        kind: "slack/conversations_inviteShared",
        params: resolveParams(params),
        config,
      });
    },
    join(params) {
      return ctx.expr({ kind: "slack/conversations_join", params: resolveParams(params), config });
    },
    kick(params) {
      return ctx.expr({ kind: "slack/conversations_kick", params: resolveParams(params), config });
    },
    leave(params) {
      return ctx.expr({ kind: "slack/conversations_leave", params: resolveParams(params), config });
    },
    list(params?) {
      return ctx.expr({
        kind: "slack/conversations_list",
        params: params != null ? resolveParams(params) : null,
        config,
      });
    },
    listConnectInvites(params?) {
      return ctx.expr({
        kind: "slack/conversations_listConnectInvites",
        params: params != null ? resolveParams(params) : null,
        config,
      });
    },
    mark(params) {
      return ctx.expr({ kind: "slack/conversations_mark", params: resolveParams(params), config });
    },
    members(params) {
      return ctx.expr({
        kind: "slack/conversations_members",
        params: resolveParams(params),
        config,
      });
    },
    open(params) {
      return ctx.expr({ kind: "slack/conversations_open", params: resolveParams(params), config });
    },
    rename(params) {
      return ctx.expr({
        kind: "slack/conversations_rename",
        params: resolveParams(params),
        config,
      });
    },
    replies(params) {
      return ctx.expr({
        kind: "slack/conversations_replies",
        params: resolveParams(params),
        config,
      });
    },
    setPurpose(params) {
      return ctx.expr({
        kind: "slack/conversations_setPurpose",
        params: resolveParams(params),
        config,
      });
    },
    setTopic(params) {
      return ctx.expr({
        kind: "slack/conversations_setTopic",
        params: resolveParams(params),
        config,
      });
    },
    unarchive(params) {
      return ctx.expr({
        kind: "slack/conversations_unarchive",
        params: resolveParams(params),
        config,
      });
    },
  };
}
