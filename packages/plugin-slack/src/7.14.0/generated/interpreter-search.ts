// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { Interpreter, TypedNode } from "@mvfm/core";
import { defineInterpreter, eval_ } from "@mvfm/core";
import type { SLACK_NODE_KINDS_SEARCH } from "./node-kinds-search";

type SlackSearchKind = (typeof SLACK_NODE_KINDS_SEARCH)[number];

interface SlackSearchBaseNode<K extends SlackSearchKind = SlackSearchKind>
  extends TypedNode<unknown> {
  kind: K;
  params?: TypedNode<Record<string, unknown>> | null;
  config: { token: string };
}

export interface SlackSearchAllNode extends SlackSearchBaseNode<"slack/search_all"> {}
export interface SlackSearchFilesNode extends SlackSearchBaseNode<"slack/search_files"> {}
export interface SlackSearchMessagesNode extends SlackSearchBaseNode<"slack/search_messages"> {}

export const NODE_TO_METHOD_SEARCH: Record<string, string> = {
  "slack/search_all": "search.all",
  "slack/search_files": "search.files",
  "slack/search_messages": "search.messages",
};

declare module "@mvfm/core" {
  interface NodeTypeMap {
    "slack/search_all": SlackSearchAllNode;
    "slack/search_files": SlackSearchFilesNode;
    "slack/search_messages": SlackSearchMessagesNode;
  }
}

interface SlackClientLike {
  apiCall(method: string, params?: Record<string, unknown>): Promise<unknown>;
}

export function createSlackSearchInterpreter(client: SlackClientLike): Interpreter {
  const handler = async function* (node: SlackSearchBaseNode) {
    const method = NODE_TO_METHOD_SEARCH[node.kind];
    if (!method) throw new Error(`Slack interpreter: unknown node kind "${node.kind}"`);
    const params = node.params != null ? yield* eval_(node.params) : undefined;
    return await client.apiCall(method, params);
  };

  return defineInterpreter<SlackSearchKind>()({
    "slack/search_all": async function* (node: SlackSearchAllNode) {
      return yield* handler(node);
    },
    "slack/search_files": async function* (node: SlackSearchFilesNode) {
      return yield* handler(node);
    },
    "slack/search_messages": async function* (node: SlackSearchMessagesNode) {
      return yield* handler(node);
    },
  });
}
