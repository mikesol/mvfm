// AUTO-GENERATED by scripts/codegen.ts — do not edit

import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsAuth } from "./types-auth";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

export function buildSlackAuth(): SlackMethodsAuth {
  return {
    teams: {
      list(params?) {
        if (params != null) return makeCExpr("slack/auth_teams_list", [liftArg(params)]);
        return makeCExpr("slack/auth_teams_list", []);
      },
    },
    revoke(params?) {
      if (params != null) return makeCExpr("slack/auth_revoke", [liftArg(params)]);
      return makeCExpr("slack/auth_revoke", []);
    },
    test(params?) {
      if (params != null) return makeCExpr("slack/auth_test", [liftArg(params)]);
      return makeCExpr("slack/auth_test", []);
    },
  };
}
