// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { Interpreter, TypedNode } from "@mvfm/core";
import { defineInterpreter, eval_ } from "@mvfm/core";
import type { SLACK_NODE_KINDS_RTM } from "./node-kinds-rtm";

type SlackRtmKind = (typeof SLACK_NODE_KINDS_RTM)[number];

interface SlackRtmBaseNode<K extends SlackRtmKind = SlackRtmKind> extends TypedNode<unknown> {
  kind: K;
  params?: TypedNode<Record<string, unknown>> | null;
  config: { token: string };
}

export interface SlackRtmConnectNode extends SlackRtmBaseNode<"slack/rtm_connect"> {}
export interface SlackRtmStartNode extends SlackRtmBaseNode<"slack/rtm_start"> {}

export const NODE_TO_METHOD_RTM: Record<string, string> = {
  "slack/rtm_connect": "rtm.connect",
  "slack/rtm_start": "rtm.start",
};

declare module "@mvfm/core" {
  interface NodeTypeMap {
    "slack/rtm_connect": SlackRtmConnectNode;
    "slack/rtm_start": SlackRtmStartNode;
  }
}

interface SlackClientLike {
  apiCall(method: string, params?: Record<string, unknown>): Promise<unknown>;
}

export function createSlackRtmInterpreter(client: SlackClientLike): Interpreter {
  const handler = async function* (node: SlackRtmBaseNode) {
    const method = NODE_TO_METHOD_RTM[node.kind];
    if (!method) throw new Error(`Slack interpreter: unknown node kind "${node.kind}"`);
    const params = node.params != null ? yield* eval_(node.params) : undefined;
    return await client.apiCall(method, params);
  };

  return defineInterpreter<SlackRtmKind>()({
    "slack/rtm_connect": async function* (node: SlackRtmConnectNode) {
      return yield* handler(node);
    },
    "slack/rtm_start": async function* (node: SlackRtmStartNode) {
      return yield* handler(node);
    },
  });
}
