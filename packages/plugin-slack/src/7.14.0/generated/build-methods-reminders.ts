// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { PluginContext } from "@mvfm/core";
import type { SlackConfig } from "./types";
import type { SlackMethodsReminders } from "./types-reminders";

export function buildSlackReminders(
  ctx: PluginContext,
  config: SlackConfig,
): SlackMethodsReminders {
  const resolveParams = (params: unknown) => (params != null ? ctx.lift(params).__node : null);

  return {
    add(params) {
      return ctx.expr({ kind: "slack/reminders_add", params: resolveParams(params), config });
    },
    complete(params) {
      return ctx.expr({ kind: "slack/reminders_complete", params: resolveParams(params), config });
    },
    delete(params) {
      return ctx.expr({ kind: "slack/reminders_delete", params: resolveParams(params), config });
    },
    info(params) {
      return ctx.expr({ kind: "slack/reminders_info", params: resolveParams(params), config });
    },
    list(params?) {
      return ctx.expr({
        kind: "slack/reminders_list",
        params: params != null ? resolveParams(params) : null,
        config,
      });
    },
  };
}
