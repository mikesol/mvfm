// AUTO-GENERATED by scripts/codegen.ts — do not edit

import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsCanvases } from "./types-canvases";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

export function buildSlackCanvases(): SlackMethodsCanvases {
  return {
    access: {
      delete(params) {
        return makeCExpr("slack/canvases_access_delete", [liftArg(params)]);
      },
      set(params) {
        return makeCExpr("slack/canvases_access_set", [liftArg(params)]);
      },
    },
    sections: {
      lookup(params) {
        return makeCExpr("slack/canvases_sections_lookup", [liftArg(params)]);
      },
    },
    create(params?) {
      if (params != null) return makeCExpr("slack/canvases_create", [liftArg(params)]);
      return makeCExpr("slack/canvases_create", []);
    },
    delete(params) {
      return makeCExpr("slack/canvases_delete", [liftArg(params)]);
    },
    edit(params) {
      return makeCExpr("slack/canvases_edit", [liftArg(params)]);
    },
  };
}
