// AUTO-GENERATED by scripts/codegen.ts — do not edit

import { isCExpr, makeCExpr } from "@mvfm/core";
import type { SlackMethodsTeam } from "./types-team";

function liftArg(value: unknown): unknown {
  if (isCExpr(value)) return value;
  if (typeof value === "string") return value;
  if (typeof value === "number") return value;
  if (typeof value === "boolean") return value;
  if (value === null || value === undefined) return value;
  if (Array.isArray(value)) {
    return makeCExpr("slack/array", value.map(liftArg));
  }
  if (typeof value === "object") {
    const pairs: unknown[] = [];
    for (const [k, v] of Object.entries(value as Record<string, unknown>)) {
      pairs.push(k, liftArg(v));
    }
    return makeCExpr("slack/record", pairs);
  }
  return value;
}

export function buildSlackTeam(): SlackMethodsTeam {
  return {
    billing: {
      info(params) {
        if (params != null) return makeCExpr("slack/team_billing_info", [liftArg(params)]);
        return makeCExpr("slack/team_billing_info", []);
      },
    },
    externalTeams: {
      disconnect(params) {
        if (params != null)
          return makeCExpr("slack/team_externalTeams_disconnect", [liftArg(params)]);
        return makeCExpr("slack/team_externalTeams_disconnect", []);
      },
      list(params) {
        if (params != null) return makeCExpr("slack/team_externalTeams_list", [liftArg(params)]);
        return makeCExpr("slack/team_externalTeams_list", []);
      },
    },
    preferences: {
      list(params?) {
        if (params != null) return makeCExpr("slack/team_preferences_list", [liftArg(params)]);
        return makeCExpr("slack/team_preferences_list", []);
      },
    },
    profile: {
      get(params?) {
        if (params != null) return makeCExpr("slack/team_profile_get", [liftArg(params)]);
        return makeCExpr("slack/team_profile_get", []);
      },
    },
    accessLogs(params?) {
      if (params != null) return makeCExpr("slack/team_accessLogs", [liftArg(params)]);
      return makeCExpr("slack/team_accessLogs", []);
    },
    billableInfo(params?) {
      if (params != null) return makeCExpr("slack/team_billableInfo", [liftArg(params)]);
      return makeCExpr("slack/team_billableInfo", []);
    },
    info(params?) {
      if (params != null) return makeCExpr("slack/team_info", [liftArg(params)]);
      return makeCExpr("slack/team_info", []);
    },
    integrationLogs(params?) {
      if (params != null) return makeCExpr("slack/team_integrationLogs", [liftArg(params)]);
      return makeCExpr("slack/team_integrationLogs", []);
    },
  };
}
