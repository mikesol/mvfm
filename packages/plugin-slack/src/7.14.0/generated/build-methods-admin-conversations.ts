// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { PluginContext } from "@mvfm/core";
import type { SlackConfig } from "./types";
import type { SlackMethodsAdminConversations } from "./types-admin-conversations";

export function buildSlackAdminConversations(
  ctx: PluginContext,
  config: SlackConfig,
): SlackMethodsAdminConversations {
  const resolveParams = (params: unknown) => (params != null ? ctx.lift(params).__node : null);

  return {
    ekm: {
      listOriginalConnectedChannelInfo(params?) {
        return ctx.expr({
          kind: "slack/admin_conversations_ekm_listOriginalConnectedChannelInfo",
          params: params != null ? resolveParams(params) : null,
          config,
        });
      },
    },
    restrictAccess: {
      addGroup(params) {
        return ctx.expr({
          kind: "slack/admin_conversations_restrictAccess_addGroup",
          params: resolveParams(params),
          config,
        });
      },
      listGroups(params) {
        return ctx.expr({
          kind: "slack/admin_conversations_restrictAccess_listGroups",
          params: resolveParams(params),
          config,
        });
      },
      removeGroup(params) {
        return ctx.expr({
          kind: "slack/admin_conversations_restrictAccess_removeGroup",
          params: resolveParams(params),
          config,
        });
      },
    },
    archive(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_archive",
        params: resolveParams(params),
        config,
      });
    },
    bulkArchive(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_bulkArchive",
        params: resolveParams(params),
        config,
      });
    },
    bulkDelete(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_bulkDelete",
        params: resolveParams(params),
        config,
      });
    },
    bulkMove(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_bulkMove",
        params: resolveParams(params),
        config,
      });
    },
    convertToPrivate(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_convertToPrivate",
        params: resolveParams(params),
        config,
      });
    },
    convertToPublic(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_convertToPublic",
        params: resolveParams(params),
        config,
      });
    },
    create(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_create",
        params: resolveParams(params),
        config,
      });
    },
    delete(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_delete",
        params: resolveParams(params),
        config,
      });
    },
    disconnectShared(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_disconnectShared",
        params: resolveParams(params),
        config,
      });
    },
    getConversationPrefs(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_getConversationPrefs",
        params: resolveParams(params),
        config,
      });
    },
    getCustomRetention(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_getCustomRetention",
        params: resolveParams(params),
        config,
      });
    },
    getTeams(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_getTeams",
        params: resolveParams(params),
        config,
      });
    },
    invite(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_invite",
        params: resolveParams(params),
        config,
      });
    },
    lookup(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_lookup",
        params: resolveParams(params),
        config,
      });
    },
    removeCustomRetention(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_removeCustomRetention",
        params: resolveParams(params),
        config,
      });
    },
    rename(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_rename",
        params: resolveParams(params),
        config,
      });
    },
    search(params?) {
      return ctx.expr({
        kind: "slack/admin_conversations_search",
        params: params != null ? resolveParams(params) : null,
        config,
      });
    },
    setConversationPrefs(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_setConversationPrefs",
        params: resolveParams(params),
        config,
      });
    },
    setCustomRetention(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_setCustomRetention",
        params: resolveParams(params),
        config,
      });
    },
    setTeams(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_setTeams",
        params: resolveParams(params),
        config,
      });
    },
    unarchive(params) {
      return ctx.expr({
        kind: "slack/admin_conversations_unarchive",
        params: resolveParams(params),
        config,
      });
    },
  };
}
