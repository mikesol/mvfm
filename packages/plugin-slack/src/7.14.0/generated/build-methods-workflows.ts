// AUTO-GENERATED by scripts/codegen.ts — do not edit

import type { PluginContext } from "@mvfm/core";
import type { SlackConfig } from "./types";
import type { SlackMethodsWorkflows } from "./types-workflows";

export function buildSlackWorkflows(
  ctx: PluginContext,
  config: SlackConfig,
): SlackMethodsWorkflows {
  const resolveParams = (params: unknown) => (params != null ? ctx.lift(params).__node : null);

  return {
    featured: {
      add(params) {
        return ctx.expr({
          kind: "slack/workflows_featured_add",
          params: resolveParams(params),
          config,
        });
      },
      list(params) {
        return ctx.expr({
          kind: "slack/workflows_featured_list",
          params: resolveParams(params),
          config,
        });
      },
      remove(params) {
        return ctx.expr({
          kind: "slack/workflows_featured_remove",
          params: resolveParams(params),
          config,
        });
      },
      set(params) {
        return ctx.expr({
          kind: "slack/workflows_featured_set",
          params: resolveParams(params),
          config,
        });
      },
    },
    stepCompleted(params) {
      return ctx.expr({
        kind: "slack/workflows_stepCompleted",
        params: resolveParams(params),
        config,
      });
    },
    stepFailed(params) {
      return ctx.expr({
        kind: "slack/workflows_stepFailed",
        params: resolveParams(params),
        config,
      });
    },
    updateStep(params) {
      return ctx.expr({
        kind: "slack/workflows_updateStep",
        params: resolveParams(params),
        config,
      });
    },
  };
}
