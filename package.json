{
  "name": "mvfm",
  "version": "0.0.1",
  "description": "Extensible tagless final DSL for deterministic, verifiable programs in TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "biome check . && tsc && api-extractor run --local",
    "format": "biome check --write .",
    "test": "vitest run",
    "size": "size-limit",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,js,json}": "biome check --write --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "@aws-sdk/client-s3": "^3.989.0",
    "@biomejs/biome": "^2.3.14",
    "@microsoft/api-extractor": "^7.56.3",
    "@size-limit/preset-small-lib": "^12.0.0",
    "@slack/web-api": "^7.14.1",
    "@testcontainers/postgresql": "^11.11.0",
    "@types/pino": "^7.0.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "pino": "^10.3.1",
    "openai": "^6.22.0",
    "pino": "^10.3.1",
    "postgres": "^3.4.8",
    "size-limit": "^12.0.0",
    "stripe": "^20.3.1",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@fal-ai/client": "^1.9.1"
  }
}
